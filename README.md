# project-marathon-ai-agent
This is the repo for live session multi ai agents are being built using langgrpah, Pyhton and FastAPI.

python3 -m pip install langchain langgraph google-genai fastapi "uvicorn[standard]"


--Install Calendar dependencies
python3 -m pip install \
  google-api-python-client \
  google-auth-httplib2 \
  google-auth-oauthlib


